import { Sample1Directive } from './sample1.directive';

describe('Sample1Directive', () => {
  it('should create an instance', () => {
    const directive = new Sample1Directive();
    expect(directive).toBeTruthy();
  });
});
