import { DirePracDirective } from './dire-prac.directive';

describe('DirePracDirective', () => {
  it('should create an instance', () => {
    const directive = new DirePracDirective();
    expect(directive).toBeTruthy();
  });
});
