import { PractDirective } from './pract.directive';

describe('PractDirective', () => {
  it('should create an instance', () => {
    const directive = new PractDirective();
    expect(directive).toBeTruthy();
  });
});
